# Certificates Folder

1. Drop your certificate images (JPG, PNG, PDF) here.
2. Example: `my-security-cert.jpg`
3. Then in `src/components/Certificates.jsx`, update the `image` path to: `/certificates/my-security-cert.jpg`
